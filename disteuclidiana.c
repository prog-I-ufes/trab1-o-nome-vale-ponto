#include <stdlib.h>
#include <stdio.h>

/*
typedef struct exemplares{
	float c1;
	float c2;
    float c3;
} Point;
*/

/*soma a potencia de duas caracteristicas pra fazer a raiz quadrada*/
float somaPotencia(exemplar e1, exemplar e2, qtdDCaracteristicas)
{
	float resultadoSP=0;
	int i=0;
	for(i=0;i<=qtdDCaracteristicas;i++)
	{
		ResultadoSP += pow(e1.c[i] - e2.c[i], 2)
	}
distanciafinal["exemplare2"]= sqrt(resultadoSP);
}

float distmink(exemplar e1, exemplar e2, qtdDCaracteristicas, r);
{
	float resultadoDM=0;
	int i=0;
	for(i=0;i<qtdDCaracteristicas;i++)
	{
		resultadoCM = pow(pow(abs(e1.c[i] - e2.c[i]),r),1/r);
	}
distanciafinal["exemplare2"]= resultadoCM;
}

float distcheby(exemplar e1, exemplare2,qtdDCaracteristicas)
{
	float resultadoDC;
	int i=0, aux=0;
	for(i=0;i<qtdDCaracteristicas;i++)
	{
		resultadoDC= x[i] - y[i]
		if (resultadoDC > aux)
		{
			aux = resultadoDC;
		}
	}

	e1.c[i] - e2.c[i]
}
int main()
{
 int distanciafinal[qtdexemplares];


}